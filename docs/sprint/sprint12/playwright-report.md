# Sprint 12 Playwright 검증 보고서

- **작성일:** 2026-03-03
- **검증 환경:** 데모 모드 (http://localhost:3001)
- **브랜치:** sprint-12 (커밋: 2749db4)

---

## 검증 항목 및 결과

### 1. KIS API 설정 페이지 렌더링

| 항목 | 결과 | 비고 |
|------|------|------|
| `/settings` 페이지 접근 | 통과 | HTTP 200 (307 리다이렉트 후 정상 접근) |
| 모의투자 앱 키 섹션 표시 | 통과 | App Key / App Secret / 계좌번호 입력란 3개 표시됨 |
| 실전투자 앱 키 섹션 표시 | 통과 | App Key / App Secret / 계좌번호 입력란 3개 표시됨 |
| 실전 키 필수 안내 문구 표시 | 통과 | "시세(현재가·차트·지수)는 실전 키로만 조회됩니다..." 문구 표시됨 |
| HTS ID 입력란 표시 | 통과 | eFriend Plus 로그인 ID 입력란 표시됨 |
| 투자 모드 RadioGroup 표시 | 통과 | 모의투자 / 실전투자 라디오 버튼 2개 표시됨 |
| 저장 버튼 표시 | 통과 | 하단 저장 버튼 표시됨 |

### 2. 비밀값 표시/숨기기 토글

| 항목 | 결과 | 비고 |
|------|------|------|
| 모의투자 App Key "표시" 버튼 클릭 | 통과 | 버튼 텍스트가 "숨기기"로 변경됨 |
| 입력란 type 변경 (password → text) | 통과 | 입력값이 평문으로 표시됨 |

### 3. 입력 및 저장 동작

| 항목 | 결과 | 비고 |
|------|------|------|
| 모의투자 App Key 입력 | 통과 | "test_vts_key_123" 정상 입력됨 |
| 실전투자 App Key 입력 | 통과 | "test_real_key_456" 정상 입력됨 |
| 저장 버튼 클릭 → API 호출 | 통과 | `PUT /api/v1/system-settings` 호출됨 |
| 데모 모드 차단 (403 Forbidden) | 통과 | 데모 모드에서는 의도적으로 저장 차단됨 |
| 데모 모드 토스트 알림 | 통과 | "데모 모드에서는 사용할 수 없습니다." 토스트 표시됨 |

### 4. 긴급 전체 매도 모달

| 항목 | 결과 | 비고 |
|------|------|------|
| 긴급 전체 매도 버튼 클릭 | 통과 | 확인 모달 표시됨 |
| 모달 제목 표시 | 통과 | "정말로 전체 매도하시겠습니까?" 표시됨 |
| 모달 경고 문구 표시 | 통과 | "모든 보유 종목을 현재가에 즉시 매도합니다. 이 작업은 되돌릴 수 없습니다." 표시됨 |
| 취소 버튼 클릭 | 통과 | 모달이 닫히고 설정 페이지 유지됨 |

### 5. API 요청 확인

| 엔드포인트 | 메서드 | 상태 | 결과 |
|-----------|--------|------|------|
| `/api/v1/system-settings` | GET | 200 OK | 통과 — 설정 데이터 정상 로드 |
| `/api/v1/safety/status` | GET | 200 OK | 통과 |
| `/api/v1/system-settings` | PUT | 403 Forbidden | 통과 (데모 모드 의도적 차단) |

### 6. 콘솔 에러 확인

데모 모드에서의 API 차단에 의한 에러 3건은 모두 의도된 동작입니다.

| 에러 메시지 | 종류 | 처리 |
|------------|------|------|
| `PUT /api/v1/system-settings` 403 | 네트워크 | 데모 모드 정상 차단 |
| `[MutationCache] 뮤테이션 에러: 데모 모드에서는 사용할 수 없습니다.` | 앱 에러 | 토스트로 사용자 안내됨 |
| `[useUpdateSettings] 설정 업데이트 실패: 데모 모드에서는 사용할 수 없습니다.` | 앱 에러 | 동일 |

기능적 에러(렌더링/라우팅/로직 오류) 없음.

---

## 스크린샷

| 파일명 | 설명 |
|--------|------|
| [settings-dual-key-full.png](settings-dual-key-full.png) | 설정 페이지 전체 — 모의투자/실전투자 키 섹션 분리 확인 |
| [settings-save-demo-toast.png](settings-save-demo-toast.png) | 저장 시 데모 모드 토스트 알림 |
| [emergency-sell-modal.png](emergency-sell-modal.png) | 긴급 전체 매도 확인 모달 |

---

## 검증 결론

Sprint 12 UI 변경 사항이 모두 정상 동작합니다.

- KIS API 설정 폼이 모의투자/실전투자 두 섹션으로 명확히 분리되어 표시됩니다.
- 각 섹션의 입력란(App Key, App Secret, 계좌번호)이 정상 동작합니다.
- 실전 키 필수 안내 문구가 명확하게 표시됩니다.
- 비밀값 표시/숨기기 토글이 정상 동작합니다.
- 투자 모드(모의/실전) 라디오 버튼이 정상 표시됩니다.

### 사용자 직접 수행 필요 항목

아래 항목은 실제 KIS API 키가 필요하여 자동 검증이 불가합니다.
`deploy.md` 섹션 15를 참고하여 수동으로 검증하세요.

- ⬜ `.env` 파일에서 `KIS_VTS_*` / `KIS_REAL_*` 듀얼 키 입력
- ⬜ `docker compose up --build` 성공 확인
- ⬜ `GET /api/v1/settings/kis-status` → `is_available: true` 확인
- ⬜ `GET /api/v1/stocks/005930/quote` → 실제 시세 데이터 반환 확인 (실전 키 필요)
- ⬜ `docker compose exec backend pytest -v` → 14개 PASSED 확인
